{
  "name": "evolvix-social-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for running all services",
  "scripts": {
    "install:all": "npm install && cd backend && npm install",
    "dev": "concurrently -c \"cyan,magenta,yellow\" -n \"BACKEND,WORKER,ML\" \"npm run dev:backend\" \"npm run dev:worker\" \"npm run dev:ml\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:worker": "cd backend && npm run worker",
    "dev:ml": "cd apps/ml && python -m uvicorn app.main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
